
all:
  hosts:
    centos2:
      ansible_host: 192.168.121.130   # IP of remote server
      ansible_user: deploy             # user to SSH as
      ansible_ssh_private_key_file: /home/user1/.ssh/id_ed25519

